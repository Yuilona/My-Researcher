# 03 Implementation Notes

## Initial Notes
- Start with 3-5 curated cases per case type when feasible.
- Store long run outputs and replay diffs as artifacts.
- Keep metrics explicit and simple before adding dashboards.

## Open Questions
- Which historical title-cards can seed the first curated dataset?
- How much of replay can use current workflow harness before full persistence lands?
- Where should frozen snapshots be stored for deterministic replay?
